<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- UNSELECTED -->
    <item android:state_checked="false">
        <layer-list>
            <!-- 이미지 영역 -->
            <item
                android:drawable="@drawable/icon_dissatisfaction_off"
                android:width="104dp" android:height="120dp" />
        </layer-list>
    </item>

    <!-- SELECTED -->
    <item android:state_checked="true">
        <layer-list>
            <!-- 이미지 영역 -->
            <item
                android:drawable="@drawable/icon_dissatisfaction_on"
                android:width="104dp" android:height="120dp" />
        </layer-list>
    </item>
</selector>